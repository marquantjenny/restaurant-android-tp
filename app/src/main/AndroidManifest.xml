<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.isen.MARQUANT.androidrestaurant">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">
        <activity android:name=".basket.BasketAdapter"></activity>
        <activity android:name=".basket.BasketActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".basket.Basket" />
        <activity android:name=".detail.PhotoAdapter" />
        <activity android:name=".detail.DetailActivity" />
        <activity android:name=".network.Price" />
        <activity android:name=".network.ingredient" />
        <activity android:name=".network.Dish" />
        <activity android:name=".network.Category" />
        <activity android:name=".network.MenuResult" />
        <activity android:name=".network.NetworkConstant" />
        <activity android:name=".CategoryAdapter" />
        <activity android:name=".category.CategoryActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>